@page "/"
@inject NavigationManager navigationManager
@using System.IO
@inject IConfiguration configuration

@code {
    protected override void OnInitialized()
    {
        navigationManager.NavigateTo("/SPY04N");
    }
}